import 'package:flutter/material.dart';

class EpisodsList extends StatelessWidget {
  const EpisodsList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Episods',
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
